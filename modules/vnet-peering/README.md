# Module: vnet-peering

Reusable Terraform module. See `variables.tf` for inputs and `outputs.tf` for outputs.
Called from the `environments/` configurations — this module has no hardcoded values
and is not meant to be applied standalone.
